import React from 'react'

export const Home = () => {
  return (
    <div>
    <li>Context is designed to share data that can be considered “global” for a tree of React components, such as the current authenticated user, theme, or preferred language. </li>
    <li>For example, in the code below we manually thread through a “theme” prop in order to style the Button component:</li>
    <li>Using context, we can avoid passing props through intermediate elements:</li>
    </div>
  )
}
